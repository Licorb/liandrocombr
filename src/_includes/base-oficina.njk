<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Crimson+Pro:wght@400;600&family=Work+Sans:wght@400;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/assets/css/estilo.css">
    <link rel="stylesheet" href="/assets/css/estilo-njk.css">
</head>
<body class="base-oficina">
    <nav>
        <div class="nav-brand">
            {% if title %}
                {{ title }} de Liandro
            {% else %}
                Liandro Ribeiro
            {% endif %}
        </div>
        <ul>
            <li><a href="../../.">Ponto de Partida</a></li>
            <li><a href="../../repertorio">Repertório</a></li>
            <li><a href="../../oficina">Oficina</a></li>
            <li><a href="../../#contato">Contato</a></li>
        </ul>
    </nav>

    <main>
        <article class="post-container">
            <div aria-label="Breadcrumb" class="breadcrumb">
                <ul style="display: flex; list-style: none; padding: 0; gap: 8px;">
                    <li><a href="/">Home</a></li>
                    
                    {# Divide a URL e remove partes vazias #}
                    {% set segments = page.url.split('/') %}
                    
                    {% set currentPath = "" %}
                    {% for segment in segments %}
                    {% if segment != "" %}
                        {% set currentPath = currentPath + "/" + segment + "/" %}
                        <li>
                        <span style="margin:0 4px; color: #999;">|</span>
                        {% if loop.last %}
                            {# Na última parte, usamos o título da página #}
                            <strong>{{ title }}</strong>
                        {% else %}
                            {# Nas partes do meio, limpamos o nome da pasta #}
                            <a href="{{ currentPath }}">{{ segment | replace("-", " ") | capitalize }}</a>
                        {% endif %}
                        </li>
                    {% endif %}
                    {% endfor %}
                </ul>
            </div>
        <h1 class="post-title">{{ title }}</h1>
            {# ===== SLIDER COM GALERIA ===== #}
            {% if image or sliderImages %}
            <div class="slider-container">
                <div class="slider-main" id="sliderMain">
                    {# Imagem principal (se existir) #}
                    {% if image %}
                    <img src="{{ image }}" alt="{{ title }}" class="slider-image active" data-index="0">
                    {% endif %}
                    
                    {# Imagens adicionais do slider (fornecidas manualmente) #}
                    {% if sliderImages %}
                    {% for item in sliderImages %}
                    <img src="{{ item.src }}" alt="{{ item.caption or 'Imagem' }}" class="slider-image" data-index="{{ loop.index }}">
                    {% endfor %}
                    {% endif %}
                </div>

                {# Controles de navegação #}
                {# <div class="slider-controls">
                    <button class="slider-arrow" id="prevBtn" aria-label="Imagem anterior">←</button>
                    <button class="slider-arrow" id="nextBtn" aria-label="Próxima imagem">→</button>
                </div> #}
            </div>

            {# ===== LAYOUT TEXTO + GALERIA ===== #}
            <div class="text-gallery-wrapper">
                <div class="text-content" id="captionContent">
                    <p id="currentCaption">
                        {% if image %}
                            {{ imageCaption or title }}
                        {% elif sliderImages and sliderImages[0] %}
                            {{ sliderImages[0].caption or title }}
                        {% else %}
                            {{ title }}
                        {% endif %}
                    </p>
                </div>

                {# Galeria de thumbnails #}
                <div class="slider-gallery" id="sliderGallery">
                    {% if image %}
                    <div class="gallery-thumbnail active" data-index="0" title="{{ imageCaption or title }}" data-caption="{{ imageCaption or title }}">
                        <img src="{{ image }}" alt="{{ title }}">
                    </div>
                    {% endif %}
                    
                    {% if sliderImages %}
                    {% for item in sliderImages %}
                    <div class="gallery-thumbnail" data-index="{{ loop.index }}" title="{{ item.caption or 'Imagem' }}" data-caption="{{ item.caption or 'Imagem' }}">
                        <img src="{{ item.src }}" alt="{{ item.caption or 'Imagem' }}">
                    </div>
                    {% endfor %}
                    {% endif %}
                </div>
            </div>
            {% endif %}

            

            

            <div class="post-meta">
                <time class="post-date" datetime="{{ date | dateIso }}">
                    {{ date | dateFilter }}
                </time>
                {% if tags and tags | length > 0 %}
                <div class="post-tags">
                    {% for tag in tags %}
                        {% if tag !== 'oficina' %}
                        <span class="post-tag">{{ tag }}</span>
                        {% endif %}
                    {% endfor %}
                </div>
                {% endif %}
            </div>

            <div class="post-content">
                {{ content | safe }}
            </div>
        </article>

        {# ===== SEÇÃO DE OUTROS TRABALHOS ===== #}
        {% if relatedWorks %}
        <section class="trabalhos-section">
            <div class="trabalhos-header">
                <h2>Outros Trabalhos</h2>
                <p>Mais referências e projetos relacionados</p>
            </div>

            <ul class="trabalhos-grid">
                {% for work in relatedWorks %}
                <li class="trabalho-card">
                    <a href="{{ work.url }}">
                        {% if work.image %}
                        <img src="{{ work.image }}" alt="{{ work.title }}" class="trabalho-thumbnail">
                        {% else %}
                        <img src="/assets/images/placeholder.jpg" alt="{{ work.title }}" class="trabalho-thumbnail">
                        {% endif %}
                    </a>
                    
                    <div class="trabalho-content">
                        {% if work.date %}
                        <time class="trabalho-date" datetime="{{ work.date | dateIso }}">
                            {{ work.date | dateFilter }}
                        </time>
                        {% endif %}
                        
                        <h3 class="trabalho-title">
                            <a href="{{ work.url }}">{{ work.title }}</a>
                        </h3>
                        
                        {% if work.description %}
                        <p class="trabalho-excerpt">{{ work.description }}</p>
                        {% endif %}
                    </div>
                </li>
                {% endfor %}
            </ul>
            <div class="voltar-container" style="margin: 20px 0;">
                <a href=".." class="btn btn-secondary">
                    &larr; Voltar para listagem
                </a>
            </div>
        </section>
        {% endif %}
    </main>

    <footer>
        <p>&copy; 2026 {{ site.author | default('Seu Nome') }}. Todos os direitos reservados.</p>
    </footer>

    <script>
        // Controle do slider
        const sliderMain = document.getElementById('sliderMain');
        const sliderGallery = document.getElementById('sliderGallery');
        const sliderIndicators = document.getElementById('sliderIndicators');
        const prevBtn = document.getElementById('prevBtn');
        const nextBtn = document.getElementById('nextBtn');

        if (sliderMain) {
            let currentIndex = 0;
            const images = sliderMain.querySelectorAll('.slider-image');
            const thumbnails = sliderGallery ? sliderGallery.querySelectorAll('.gallery-thumbnail') : [];
            const indicators = sliderIndicators ? sliderIndicators.querySelectorAll('.indicator') : [];
            const totalImages = images.length;

            function showImage(index) {
                // Garante que o índice está dentro do intervalo
                if (index < 0) index = totalImages - 1;
                if (index >= totalImages) index = 0;
                currentIndex = index;

                // Atualizar imagens
                images.forEach((img, i) => {
                    img.classList.toggle('active', i === currentIndex);
                });

                // Atualizar thumbnails
                thumbnails.forEach((thumb, i) => {
                    thumb.classList.toggle('active', i === currentIndex);
                });

                // Atualizar indicadores
                indicators.forEach((ind, i) => {
                    ind.classList.toggle('active', i === currentIndex);
                });

                updateCaption(currentIndex);
            }

            function updateCaption(index) {
                const captionElement = document.getElementById('currentCaption');
                const thumbnail = thumbnails[index];
                if (!captionElement || !thumbnail) return;
                captionElement.textContent = thumbnail.dataset.caption || '';
            }

            // Eventos dos botões de navegação
            if (prevBtn) {
                prevBtn.addEventListener('click', () => showImage(currentIndex - 1));
            }
            if (nextBtn) {
                nextBtn.addEventListener('click', () => showImage(currentIndex + 1));
            }

            // Eventos dos thumbnails
            thumbnails.forEach((thumbnail, index) => {
                thumbnail.addEventListener('click', () => showImage(index));
            });

            // Eventos dos indicadores
            indicators.forEach((indicator, index) => {
                indicator.addEventListener('click', () => showImage(index));
            });

            // Navegação com teclado
            document.addEventListener('keydown', (e) => {
                if (e.key === 'ArrowLeft') showImage(currentIndex - 1);
                if (e.key === 'ArrowRight') showImage(currentIndex + 1);
            });
        }
    </script>
</body>
</html>
